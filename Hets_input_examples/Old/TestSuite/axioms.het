logic Hybrid

spec Axioms = 
	preds p,q : ()
	modality M
	nominal N
	. [M] (p /\ q) => ([M] p /\ [M] q) %(MDist)%
	. @N (p /\ q) => (@N p /\ @N q) %(HDist)%
	. ! X (Here X) %(Ref)%
	. (Here N /\ p) => @N p %(Intro)%  
end
